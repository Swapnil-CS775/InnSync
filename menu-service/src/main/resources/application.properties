# Server Configuration (must run on a different port)
server.port=8081
spring.application.name=menu-service

# =========================
# === Database Settings ===
# =========================
# === Primary DataSource (Registry DB) ===
app.datasource.primary.url=jdbc:mariadb://localhost:3306/registry
app.datasource.primary.username=root
app.datasource.primary.password=swapnil@92
app.datasource.primary.driver-class-name=org.mariadb.jdbc.Driver

# --- Tenant Datasource (Template for tenant-specific DBs) ---
app.datasource.tenant.url-template=jdbc:mariadb://localhost:3306/{dbName}
app.datasource.tenant.username=root
app.datasource.tenant.password=swapnil@92
app.datasource.tenant.driver-class-name=org.mariadb.jdbc.Driver


# =========================
# === JPA & Hibernate ===
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# JWT Configuration
jwt.secret.key=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437